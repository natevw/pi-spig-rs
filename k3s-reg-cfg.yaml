# sudo mkdir -p /etc/rancher/k3s
# sudo vi /etc/rancher/k3s/registries.yaml
# sudo systemctl restart k3s-agent


# kubectl label nodes turing-node-0.lan localreg=ready
# kubectl label nodes turing-node-1.lan localreg=ready



mirrors:
  turing-node-x.lan:5000:
    endpoint:
      - "http://turing-node-x.lan:5000"

configs:
  my-insecure-registry.example.com:5000:
    tls:
      insecure_skip_verify: true
